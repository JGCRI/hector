# Configuration file for the lintr R package, used by GitHub Actions and perhaps locally

linters: with_defaults(
  object_name_linter = NULL,
  line_length_linter(100)
  )
exclusions: list("R/RcppExports.R")
