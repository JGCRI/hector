name: Leeyabot Report

on:
  pull_request:
    branches:
      - '**' # matches every branch

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: iterative/setup-cml@v1
      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: 'release'

      - name: Install libcurl
        run: sudo apt-get install libcurl4-openssl-dev

      - name: Install dependencies
        env:
          GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
        run: |
          install.packages(c("ggplot2", "rmarkdown", "dplyr", "remotes", "here"))
          remotes::install_deps(dependencies = TRUE)
        shell: Rscript {0}

      - name: Install package
        run: R CMD INSTALL .

      - name: Generate report
        run: |
          rmarkdown::render("./scripts/leeyabot.Rmd", run_pandoc = FALSE)
        shell: Rscript {0}

      - name: Post comment
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
            # Post reports as comments in GitHub PRs
            cd scripts/figs
            if [ -f "01_rslts.png" ]
            then
                cat leeyabot.knit.md >> report.md
                echo "![](./01_rslts.png)" >> report.md
                echo "![](./01_diff_time.png)" >> report.md
            elif [ -f "same_plot.png" ]
            then
                cat leeyabot.knit.md >> report.md
            else
                cat leeyabot.knit.md >> report.md
                echo "leeyabot down :(" > report.md
            fi
            cml comment create report.md
